const db = require('../db')

const addTeacher = async (r, h) => {

}

const delTeacher = (r, h) => {

}

const getTeacher = (r, h) => {

}

const editTeacher = (r, h) => {
    
}

module.exports = {
    addTeacher,
    delTeacher,
    getTeacher,
    editTeacher
}